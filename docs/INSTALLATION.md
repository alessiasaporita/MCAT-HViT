Use the environment configuration file located in **docs/mcat.yaml** to create a conda environment:
```shell
conda env create -n mcat -f docs/requirements.yaml
```

Activate the environment:
```shell
conda activate MCAT
```
